# 引擎全量审计 2026-07-04

> 本文件记录2026-07-04对引擎代码 vs 技能规则 vs 九龙道长原始理论的全量交叉审计过程与发现。
> 审计方法：对各核心规则，同时在引擎代码(`engine/*.py`)、技能描述(`skills/bazi/*/SKILL.md`)、原始素材(KB `01-理论体系/00-原始素材/`)三处搜索验证。

## 审计方法

```
逐规则三源交叉验证:
  规则A
    ├─ 引擎代码 → 是否实现了该规则？（计算逻辑是否正确？）
    ├─ 技能描述 → 是否正确描述了该规则？（表述是否与原文一致？）
    └─ 原始素材 → 九龙道长原文是否支持该规则？（行号可溯？）
```

## 审计发现汇总

| # | 规则 | 引擎 | 技能 | 原始素材 | 状态 |
|:-:|:-----|:----:|:----:|:--------:|:----:|
| 1 | 月令本气印=40分 | ✅ `shen_qiang_ruo.py:143` | ✅ `foundation:1139` | ✅ 素材20行1038 | ✅ |
| 2 | 月令中/余气印=0分 | ✅ `shen_qiang_ruo.py:148` | ✅ `foundation:1139` | ✅ 同上 | ✅ |
| 3 | 比劫全计分 | ✅ `shen_qiang_ruo.py:120-135` | ✅ `foundation:1138` | ✅ 素材09行89 | ✅ |
| 4 | 燥土规则(未/戌+丙丁) | ✅ `shen_qiang_ruo.py:50-67` | ✅ `foundation:1222` | ✅ 实战校准 | ✅ |
| 5 | 从弱恒定50分 | ✅ `shen_qiang_ruo.py:266` | ✅ `foundation:1273` | ✅ | ✅ |
| 6 | 藏干100/60/30 | ✅ `constants.py` | ✅ `foundation:651` | ✅ | ✅ |
| 7 | 自坐比劫永不从弱 | ✅ **已修复** `shen_qiang_ruo.py:262-272` | ✅ `foundation:1395-1398` | ✅ 九龙道长口诀 | ✅ |
| 8 | 月令被克三次递减 | ✅ **已修复** `shen_qiang_ruo.py:70-100` | ✅ `foundation:554-565` | ✅ 2026-07-04配婚课 | ✅ |
| 9 | 门禁脚本调用完整引擎 | ✅ **已修复** `bazi-must-run-engine.sh` | ⚠️ 技能引用`bazi-pipeline.sh`不存在 | — | ✅ |

## 修复记录

### 修复①：自坐比劫永不从弱（2026-07-04审计触发）

**发现：** 引擎 `shen_qiang_ruo.py` 在总分≤0时直接标为「从弱」，未检查日支是否比劫。

**修复：** 在 `compute_shen_qiang_ruo()` 中添加日支藏干检查：
```python
if total <= 0 and ri_zhi_bi_jie:
    label = "身弱"  # 自坐比劫→永不从弱
elif total <= 0:
    label = "从弱"  # 原逻辑
```

### 修复②：月令被克三次递减（2026-07-04配婚课校准）

**发现：** 技能 `foundation-analysis v2.15` 有规则，引擎未实现。

**修复：** 添加 `_count_yue_bei_ke()` 函数 + 分数调整。

### 修复③：门禁脚本只排盘不评分（2026-07-04审计）

**发现：** `bazi-must-run-engine.sh` 只调 `paipan.py.get_full_paipan()`，无引擎评分。

**修复：** 添加 `run_v5()` 调用，输出 `engine_scores` 字段。

## 审计清单（每次引擎修改后运行）

```bash
# 对每条核心规则，检查三个来源一致：
# 引擎是否实现？技能是否正确描述？原文是否支持？
./scripts/engine_audit_checklist.sh  # TODO: 待创建
```

## 建议后续

- [ ] 创建 `engine_audit_checklist.sh` 脚本 → 自动运行所有核心规则的交叉验证
- [ ] 合化能量倍数表(§20) 是否应入引擎？当前仅存于技能，无引擎计算
- [ ] 文昌贵人查法双轨制（年干命理/日干补）是否应统一在引擎中？
