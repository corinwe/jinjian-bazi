# 食伤引擎(shi_shang.py)审计 · 2026-07-05

## 审计范围

`/root/bazi-platform/engine/shi_shang.py` — 食伤评分引擎 v1.0 (2026-07-01新增)

## 审计方法

遵循铁律⑥ Step 1~7：

1. **规则审计** → 加载 `bazi-foundation-analysis` 的 `references/食伤评分完整规则速查_20260701.md(v2.0)`
2. **直接对比代码逻辑 vs 原始理论规则**
3. **引用链验证** → 搜索全项目 `from shi_shang` / `import shi_shang`
4. **自测** → `python3 shi_shang.py` 输出 vs 参考值

## 审计发现

### ✅ 算法正确性 — PASS

| 案例 | 引擎输出 | 参考值(v2.0) | 匹配 |
|:----|:---------:|:------------:|:----:|
| 家主(辛→水) | 比劫24+食伤26.4=50.4中等 | 24+26.4=50.4中等 | ✅ |
| 主母(庚→水) | 比劫0+食伤12=12极弱 | 0+12=12极弱 | ✅ |
| 子源(丙→土) | 比劫55.6+食伤43.2=98.8极旺 | 55.6+43.2=98.8极旺 | ✅ |
| 祖母(癸→木) | 比劫33.2+食伤2.4=35.6中等 | 33.2+2.4=35.6中等 | ✅ |

关键正确点：
- ✅ 食伤=比劫生食伤+食伤本身（2026-07-01校准）
- ✅ 比劫生食伤机制（天干+藏干双扫描）
- ✅ 位置基础分与身强弱/财星一致
- ✅ 月令权重40分，藏干100%/60%/30%
- ✅ 等级阈值边界与参考值完全对齐

### 🚨 引用链断裂 — 孤岛模块

**零调用者**: `calc_shi_shang_score` 全项目无任何import引用。

| 文件 | import? |
|:----|:-------:|
| `comprehensive_v2.py` | ❌ 不导入 |
| `pipeline_v5.py` | ❌ 不导入 |
| `energy.py` | ❌ 不导入 |
| `pipeline_v2/v3/v4.py` | ❌ 不导入 |
| 任何其他 `.py` | ❌ 零引用 |

**影响**: 食伤评分数据不存在于任何报告输出中。这是 **第二个** 发现引用链断裂的模块（上一个是 `education.py`）。

### 🟢 次要问题

- `WX_CN` 字典(L29)定义但未使用 — 无害死代码

## 推论

**算法正确 ≠ 功能可用。** 全链须通：`src → import → call → pipeline → report_output`。

审计 Step 3（引用链验证）必须包含 `grep -r "import\|from"` 确认 import 存在，不能只看函数定义和输出正确。
